/*Non usare, solo template */
/*
import { Router } from "express";
import * as nomeController from "../controllers/nome-controller"

const router: Router = Router()

router.get("/api/categoria/nome", nomeController.nomeFunzione)

export default router*/